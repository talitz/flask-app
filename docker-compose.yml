services:
  web:
    image: flask-app:latest
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - nginx

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf